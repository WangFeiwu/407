<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.flydou.rubbish407.dao.NoteList">
	<resultMap type="cn.flydou.rubbish407.entity.Note" id="List_note">
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="sender" jdbcType="VARCHAR" property="sender" />
		<result column="isProcess" jdbcType="BOOLEAN" property="isProcess" />
		<result column="startTime" jdbcType="VARCHAR" property="startTime" />
		<result column="endTime" jdbcType="VARCHAR" property="endTime" />
		<result column="result" jdbcType="BOOLEAN" property="result" />
	</resultMap>

	<select id="queryNote" parameterType="INTEGER" resultMap="List_note">
		select * from note where id = #{id}
	</select>

	<insert id="createNewNote" parameterType="cn.flydou.rubbish407.entity.Note" useGeneratedKeys="true" keyProperty="id" >
		insert into note values
(#{id},#{sender},#{isProcess},#{startTime},#{endTime},#{result})
	</insert>

	<delete id="deleteCourseById" parameterType="cn.flydou.rubbish407.entity.Note" flushCache="true">
		delete from note where id = #{id}
	</delete>

	<update id="updateCourse" parameterType="cn.flydou.rubbish407.entity.Note" resultMap="List_note">
		update note set courseName = sender = #{new_sender},isProcess =#{new_isProcess},startTime =#{new_startTime},endTime =#{new_endTime},result =#{new_result} where id = #{id}
	</update>
</mapper>